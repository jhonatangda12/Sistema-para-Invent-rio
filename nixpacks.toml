[phases.setup]
nixPkgs = ["php83", "php83-extensions", "composer"]
phpExtensions = ["mbstring", "pdo", "dom", "xml", "zip", "gd"]

[phases.build]
cmds = ["composer install --optimize-autoloader --no-scripts --no-interaction --ignore-platform-req=ext-gd"]